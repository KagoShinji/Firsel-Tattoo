const config = {
    plugins: {
        '@tailwindcss/postcss': {},
        autoprefixer: {}, // autoprefixer is usually included but good to be explicit or remove if causing issues
    },
};

export default config;
